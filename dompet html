<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Pohon Kebaikan | Master Pro</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <style>
        :root {
            --dana-blue: #108ee9;
            --grass-green: #2ecc71;
            --sky-blue: #e3f2fd;
        }
        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
        body { background: #f0f4f8; overflow-x: hidden; }

        /* Area Langit & Pohon */
        .garden-area {
            width: 100%;
            height: 400px;
            background: linear-gradient(to bottom, #4facfe 0%, #00f2fe 100%);
            position: relative;
            display: flex;
            justify-content: center;
            align-items: flex-end;
            overflow: hidden;
            border-bottom: 8px solid #8d6e63; /* Tanah */
        }

        /* Awan Bergerak */
        .cloud {
            position: absolute;
            color: white;
            opacity: 0.8;
            animation: moveClouds 20s linear infinite;
        }

        @keyframes moveClouds {
            from { left: -50px; }
            to { left: 100%; }
        }

        /* Visual Pohon Berdasarkan Level */
        .tree-container {
            z-index: 10;
            margin-bottom: -10px;
            transition: all 1s ease;
            text-align: center;
        }

        .tree-icon {
            font-size: 150px; /* Ukuran default */
            color: #2e7d32;
            filter: drop-shadow(0 10px 15px rgba(0,0,0,0.2));
            transition: 0.5s transform cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        
        .tree-container:active { transform: scale(1.1); }

        /* Info Dashboard */
        .stats-container {
            padding: 25px 20px;
            background: white;
            border-radius: 30px 30px 0 0;
            margin-top: -30px;
            position: relative;
            z-index: 20;
            box-shadow: 0 -10px 20px rgba(0,0,0,0.1);
            min-height: 400px;
        }

        .level-badge {
            background: var(--grass-green);
            color: white;
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 12px;
            font-weight: 700;
            display: inline-block;
            margin-bottom: 10px;
        }

        .progress-box {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 20px;
            margin-top: 10px;
        }

        .bar-bg { width: 100%; height: 12px; background: #eee; border-radius: 10px; margin: 10px 0; overflow: hidden;}
        .bar-fill { height: 100%; background: var(--grass-green); border-radius: 10px; transition: 1s width; }

        .btn-water {
            width: 100%;
            padding: 18px;
            background: var(--dana-blue);
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 16px;
            font-weight: 800;
            margin-top: 25px;
            cursor: pointer;
            box-shadow: 0 8px 20px rgba(16, 142, 233, 0.4);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
        }

        .back-btn {
            position: absolute;
            top: 40px;
            left: 20px;
            background: rgba(255,255,255,0.3);
            padding: 10px 15px;
            border-radius: 50px;
            color: white;
            text-decoration: none;
            backdrop-filter: blur(5px);
            z-index: 100;
        }
    </style>
</head>
<body>

    <a href="index.html" class="back-btn"><i class="fas fa-arrow-left"></i> Kembali</a>

    <div class="garden-area">
        <i class="fas fa-cloud cloud" style="top: 80px; font-size: 40px; animation-duration: 25s;"></i>
        <i class="fas fa-cloud cloud" style="top: 150px; font-size: 30px; animation-duration: 18s; animation-delay: -5s;"></i>
        
        <div class="tree-container" id="tree-stage">
            <i class="fas fa-seedling tree-icon" id="main-tree"></i>
        </div>
    </div>

    <div class="stats-container">
        <div class="level-badge" id="level-name">Level 1: Bibit Kebaikan</div>
        <h2 style="font-size: 24px; font-weight: 800; color: #333;">Pohon Kebaikan Kamu</h2>
        <p style="font-size: 13px; color: #777;">Tingkatkan donasi untuk menumbuhkan pohonmu menjadi rimbun dan berbuah pahala.</p>

        <div class="progress-box">
            <div style="display: flex; justify-content: space-between; font-size: 12px; font-weight: 700;">
                <span>Progress Tumbuh</span>
                <span id="percent-text">20%</span>
            </div>
            <div class="bar-bg">
                <div class="bar-fill" id="progress-bar" style="width: 20%;"></div>
            </div>
            <p style="font-size: 11px; color: #888;" id="exp-text">Butuh Rp 80.000 lagi untuk naik level.</p>
        </div>

        <button class="btn-water" onclick="location.href='kartu.html'">
            <i class="fas fa-tint"></i> SIRAM DENGAN SEDEKAH
        </button>

        <div style="margin-top: 30px; border-top: 1px solid #eee; padding-top: 20px;">
            <h4 style="font-size: 14px; margin-bottom: 10px;">Manfaat Pohon Kebaikan:</h4>
            <ul style="font-size: 12px; color: #666; padding-left: 15px; line-height: 1.8;">
                <li>Menjadi saksi amal jariyah di akhirat.</li>
                <li>Membantu renovasi dan operasional yayasan.</li>
                <li>Setiap daun mewakili keberkahan bagi donatur.</li>
            </ul>
        </div>
    </div>

    <script>
        // Logika Sederhana untuk Simulasi Tumbuh
        function updateTree() {
            // Kita ambil dari saldo (simulasi)
            let dataRaw = localStorage.getItem('kas_data');
            let listKas = dataRaw ? JSON.parse(dataRaw) : [];
            let totalDonasi = 0;
            listKas.forEach(t => { if(t.type === 'masuk') totalDonasi += parseInt(t.amount); });

            const tree = document.getElementById('main-tree');
            const levelText = document.getElementById('level-name');
            const bar = document.getElementById('progress-bar');
            const percent = document.getElementById('percent-text');

            if (totalDonasi < 100000) {
                tree.className = "fas fa-seedling tree-icon";
                tree.style.color = "#81c784";
                levelText.innerText = "Level 1: Bibit Kebaikan";
            } else if (totalDonasi < 500000) {
                tree.className = "fas fa-potted-plant tree-icon";
                tree.style.color = "#4caf50";
                levelText.innerText = "Level 2: Tunas Harapan";
            } else {
                tree.className = "fas fa-tree tree-icon";
                tree.style.color = "#2e7d32";
                levelText.innerText = "Level 3: Pohon Rimbun";
                tree.style.fontSize = "200px";
            }
            
            // Update bar (maksimal simulasi level 1 ke 2 di 100rb)
            let p = Math.min((totalDonasi / 100000) * 100, 100);
            bar.style.width = p + "%";
            percent.innerText = Math.floor(p) + "%";
        }

        window.onload = updateTree;
    </script>
</body>
</html>
